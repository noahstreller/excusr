import * as React from 'react';
import { Appbar } from 'react-native-paper';

const Header = () => (
  <Appbar.Header>
    <Appbar.Content title="Excusr" />
  </Appbar.Header>
);

export default Header;